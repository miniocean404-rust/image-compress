{
  "package": "@@giegie/core-wasm",
  "license": "MIT",
  "version": "1.0.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "scripts": {
    "build:wasm-dev": "pnpm run pack build ./ --dev --out-dir ./dist -p binding_compress_wasm",
    "build:wasm-release": "npm-run-all \"pack -- build ./ --release --out-dir ./dist\" --",
    "pack": "wasm-pack"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "npm-run-all": "^4.1.5"
  }
}
