use image_compress::compress::ImageCompress;
use image_compress_core::codecs::png::oxipng::OxiPngOptions;

#[test]
fn oxipng_compress() {
    // ImageCompress::new(vec![], 80).with_oxipng_options(options)
}
