{
    "package": "awesome-package",
    "napi": {
        "name": "miniocean_image_compress"
    },
    "scripts": {
        "pack": "wasm-pack",
        "build:wasm": "npm-run-all \"pack -- build ./crates/wasm \" --",
        "build": "napi build --release",
        "build:node:dev": " napi build  --manifest-path ./Cargo.toml --platform -p node --js ./node-swc/src/binding.js --dts ./node-swc/src/binding.d.ts -o ."
    },
    "devDependencies": {
        "@napi-rs/cli": "^3.0.0-alpha.22",
        "npm-run-all": "^4.1.5"
    }
}
